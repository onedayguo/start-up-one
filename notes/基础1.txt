callback回调功能
 hook 

模板方法模式+
说白了就是多态，面向对象的应用

定义接口名前加I表示是接口，习惯

内部类：把一个类定义在另一个类的内部，称为内部类

内部类提供了更好的封装，只能让外部类直接访问，不允许同一个包中的其他类直接访问
内部类

数组是相同数据的有序集合
数组也是对象，数组元素相当于对象的成员变量
数组的程度是确定的，不可变的，如果越界，则报越界异常
 数组声明   int【】 a 或者int a[]

sringbuilder buffer 可变字符数组  append追加  容量不够后会新建一个数组，把原来的内容复制过来 是原来的2倍+2

ArrayList
模拟实习jdk中提供的ArrayList类
 
多维数组
很多情况下用一维，最多用到二维，三维用的情况很少

包装类和基本数据类型对应，也有8种
int   integer包装类

子类声明的异常范围不能超过父类声明的范围
























